# Core
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0  # SealedSettings base (shielva_common.config.sealed) — used by the test import shim
python-dotenv>=1.0.0

# Private shared library (same pin as the Dockerfile). Declared here so the CI
# test stage's strict `pip install -r requirements.txt` + import/boot smoke can
# resolve `import shielva_common` — the smoke imports every src submodule before
# pytest, so the real dep must be installable (conftest's sys.modules shim only
# applies under pytest). Resolved via the CI git-auth token.
shielva-common @ git+https://github.com/Shielva-AI/shielva-platform-core@shielva-common-v1.2.1#subdirectory=shielva-common

# Embedding & LLM
litellm>=1.10.0
google-generativeai>=0.3.0

# Vector Store
vecs>=0.4.0
supabase>=2.0.0

# Document Parsing
pymupdf>=1.23.0  # PDF parsing
python-docx>=1.0.0  # DOCX parsing
beautifulsoup4>=4.12.0  # HTML parsing
lxml>=5.0.0  # XML/HTML parsing

# HTTP Client
httpx>=0.26.0

# Object storage (R2 / S3-compatible) — streaming fetch of direct-uploaded files
boto3>=1.34.0

# Logging
structlog>=24.1.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0  # coverage.xml for the Sonar quality gate
openpyxl>=3.1.0  # XLSX parsing
python-pptx>=0.6.23  # PPTX parsing
python-multipart>=0.0.6  # file uploads
